function main(input) {
  const args = input.split("\n");
  const [n, s, t] = args.shift().split(" ").map(Number);
  const arg2 = args.map((item) => item.split(" ").map(Number));

  console.log(b);
}

main(require("fs").readFileSync("/dev/stdin", "utf8"));
